import seaborn.objects as so
from gapminder import gapminder

def plot():
    figura = (
        so.Plot(
        )
    )
    return dict(
        descripcion="",
        autor="Bautista Gilardon",
        figura=figura,
    )